# Application Configuration
APP_PORT=8080
APP_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=hanzo
DB_PASSWORD=WVO574bJJAtr
DB_NAME=portfolioed

# Docker Configuration
IN_DOCKER=false

# Authentication Configuration
TOKEN_SYMMETRIC_KEY=9y$B&E)H@McQfTjWnZr4u7x!A%D*G-Ka
ACCESS_TOKEN_DURATION=5m
REFRESH_TOKEN_DURATION=720h

# AI Configuration
AI_API_KEY=""
AI_API_BASE_URL=https://openrouter.ai/api/v1
AI_MODEL=google/gemini-2.0-flash-001

# Performance Configuration
# Simple resource loading strategy to avoid duplicate downloads
# - CDN versions are centrally managed in internal/config/config.go GetCDNVersions()
# - All resources loaded via normal script/link tags (no preload complexity)
# - Critical scripts (HTMX) loaded immediately, others deferred
# - Development mode includes duplicate detection in browser console
# - Preload middleware temporarily disabled to fix duplicate loading issue